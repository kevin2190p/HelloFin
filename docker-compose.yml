version: "3.9"

# ============================================================
# Fakeout – Docker Compose (TNG Digital FINHACK 2026)
# Services: FastAPI, Redis, n8n, PostgreSQL (optional)
# ============================================================

services:
  # ----------------------------------------------------------
  # FastAPI Backend
  # ----------------------------------------------------------
  fastapi:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: fakeout-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./audios:/app/audios
      - ./logs:/app/logs
    depends_on:
      - redis
    networks:
      - fakeout-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ----------------------------------------------------------
  # Redis – In-memory store for transactions & risk data
  # ----------------------------------------------------------
  redis:
    image: redis:7-alpine
    container_name: fakeout-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes --requirepass ""
    networks:
      - fakeout-net

  # ----------------------------------------------------------
  # n8n – Workflow orchestration
  # ----------------------------------------------------------
  n8n:
    image: n8nio/n8n:latest
    container_name: fakeout-n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=${N8N_BASIC_AUTH_USER:-admin}
      - N8N_BASIC_AUTH_PASSWORD=${N8N_BASIC_AUTH_PASSWORD:-fakeout2026}
      - N8N_HOST=${N8N_HOST:-0.0.0.0}
      - N8N_PORT=5678
      - WEBHOOK_URL=http://n8n:5678/
      - GENERIC_TIMEZONE=Asia/Kuala_Lumpur
    volumes:
      - n8n-data:/home/node/.n8n
      - ./n8n:/home/node/workflows
    depends_on:
      - fastapi
    networks:
      - fakeout-net

  # ----------------------------------------------------------
  # Svelte Frontend (Caregiver Dashboard)
  # ----------------------------------------------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: fakeout-dashboard
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - VITE_API_BASE_URL=http://localhost:8000
    depends_on:
      - fastapi
    networks:
      - fakeout-net

  # ----------------------------------------------------------
  # PostgreSQL (optional – for persistent audit logs)
  # ----------------------------------------------------------
  postgres:
    image: postgres:16-alpine
    container_name: fakeout-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-fakeout}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-fakeout_secure_2026}
      - POSTGRES_DB=${POSTGRES_DB:-fakeout_db}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - fakeout-net
    profiles:
      - with-postgres

volumes:
  redis-data:
  n8n-data:
  pgdata:

networks:
  fakeout-net:
    driver: bridge
